/**
 * MQTT integration to make IoT messaging out of the box.
 * Handles everything server would need to do for getting raw data
 * and to forward for consumers the actual location data after trilateration.
 */
package fi.helsinki.ubipositioning.mqtt;
